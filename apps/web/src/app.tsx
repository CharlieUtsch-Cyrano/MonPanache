/**
 * Placeholder root. The real shell (rail, ⌘K palette, activity feed, theme)
 * is Bolt 2 — ported from the house patterns, not invented here.
 */
export function App() {
  return (
    <main>
      <h1>Cyrano-Task-Manager</h1>
      <p>Charter is in. The shell lands in Bolt 2.</p>
    </main>
  );
}
